{ 
  "scripts": {
  "test": "echo \"Error: no test specified\" && exit 1",
  "dev" : "npx tailwindcss -i ./src/css/input.css -o ./src/css/style.css --watch" 
  },
  "devDependencies": {
    "tailwindcss": "^3.3.3"
  }
}
